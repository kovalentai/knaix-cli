name: Download stats

# Counts CLI installs from the releases.knaix.com CloudFront access logs and
# publishes the result to releases.knaix.com/stats.json, which is the payload
# behind the shields.io endpoint badge and the download figure on the sites.
#
# GitHub cannot supply this number: knaix-cli is private, so shields.io reads
# neither the repo nor its release assets, and both install paths (install.sh
# and the Homebrew tap) fetch from S3 rather than GitHub Releases anyway.
#
# Required repository variables:
#   AWS_STATS_ROLE_ARN              knaix-cli-github-actions-stats role
#   KNAIX_RELEASES_DISTRIBUTION_ID  CloudFront distribution for releases.knaix.com
# Both are Terraform outputs in kovalent-terraform accounts/sandbox/us-west-2.

on:
  schedule:
    # 00:17 PT. Off the hour, since scheduled runs queue behind everyone
    # else's on-the-hour crons.
    - cron: '17 7 * * *'
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

concurrency:
  group: download-stats
  cancel-in-progress: false

env:
  AWS_REGION: us-west-2
  ATHENA_WORKGROUP: knaix-releases-stats
  RELEASES_BUCKET: knaix-releases

jobs:
  publish:
    name: Count and publish
    runs-on: ubuntu-latest
    steps:
      - name: Check required variables
        env:
          ROLE_ARN: ${{ vars.AWS_STATS_ROLE_ARN }}
          DISTRIBUTION_ID: ${{ vars.KNAIX_RELEASES_DISTRIBUTION_ID }}
        run: |
          set -euo pipefail
          [ -n "$ROLE_ARN" ] || { echo "::error::AWS_STATS_ROLE_ARN is not set"; exit 1; }
          [ -n "$DISTRIBUTION_ID" ] || { echo "::error::KNAIX_RELEASES_DISTRIBUTION_ID is not set"; exit 1; }

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_STATS_ROLE_ARN }}
          aws-region: us-west-2

      # A download is a successful GET of a versioned binary. /latest-version
      # is deliberately excluded: every installed CLI polls it on a timer, so
      # counting it would measure the installed base's update checks, not
      # installs. 206 is counted because a resumed download still lands one
      # copy; the deduplicated figure is what absorbs the double-count.
      - name: Query Athena
        id: query
        run: |
          set -euo pipefail

          # The counts are filtered to downloads, but the freshness probe
          # deliberately is not. Freshness has to answer "is CloudFront still
          # delivering logs", and a quiet week with no installs is not the same
          # failure as log delivery having stopped. Every installed CLI polls
          # /latest-version on a timer, so unfiltered traffic keeps flowing as
          # long as an installed base exists, which makes it the honest probe.
          SQL=$(cat <<'SQL'
          SELECT
            count_if(is_download) AS total,
            count(DISTINCT if(is_download, dedup_key)) AS deduped,
            cast(max("date") AS varchar) AS latest
          FROM (
            SELECT
              "date",
              method = 'GET'
                AND status IN (200, 206)
                AND regexp_like(uri, '^/v[0-9]+\.[0-9]+\.[0-9]+/knaix-(darwin|linux)-(arm64|x86_64)$')
                AS is_download,
              concat_ws('|', request_ip, user_agent, cast("date" AS varchar)) AS dedup_key
            FROM knaix_releases.cloudfront_logs
          )
          SQL
          )

          QID=$(aws athena start-query-execution \
            --query-string "$SQL" \
            --work-group "$ATHENA_WORKGROUP" \
            --query QueryExecutionId --output text)
          echo "Query $QID"

          for _ in $(seq 1 60); do
            STATE=$(aws athena get-query-execution --query-execution-id "$QID" \
              --query 'QueryExecution.Status.State' --output text)
            case "$STATE" in
              SUCCEEDED)
                break
                ;;
              FAILED|CANCELLED)
                REASON=$(aws athena get-query-execution --query-execution-id "$QID" \
                  --query 'QueryExecution.Status.StateChangeReason' --output text)
                echo "::error::Athena query $STATE: $REASON"
                exit 1
                ;;
            esac
            sleep 5
          done

          if [ "$STATE" != "SUCCEEDED" ]; then
            echo "::error::Athena query did not finish within five minutes"
            exit 1
          fi

          # Row 0 is the header; row 1 holds the values, tab separated. A NULL
          # is omitted from the array rather than returned empty, so an empty
          # table yields two values instead of three and latest reads empty.
          # The counts are never NULL, so only the last column can shift.
          ROW=$(aws athena get-query-results \
            --query-execution-id "$QID" \
            --query 'ResultSet.Rows[1].Data[*].VarCharValue' --output text)
          TOTAL=$(printf '%s' "$ROW" | cut -f1)
          DEDUPED=$(printf '%s' "$ROW" | cut -f2)
          LATEST=$(printf '%s' "$ROW" | cut -f3)

          echo "total=$TOTAL" >> "$GITHUB_OUTPUT"
          echo "deduped=$DEDUPED" >> "$GITHUB_OUTPUT"
          echo "latest=$LATEST" >> "$GITHUB_OUTPUT"
          echo "Total $TOTAL, deduplicated $DEDUPED, newest log entry ${LATEST:-none}"

      # Without this, a break in log delivery does not fail. It just stops the
      # number growing, and the badge shows a plausible figure forever. AWS
      # documents that the awslogsdelivery grant can be reset, and that logs
      # then stop silently, so the stale-but-believable case is the one to
      # engineer against: a wrong number nobody questions beats a broken badge
      # only from the perspective of the badge.
      - name: Check log freshness
        env:
          LATEST: ${{ steps.query.outputs.latest }}
          # Entries can arrive up to 24 hours late, so this is two documented
          # delays plus a day, not an SLA.
          MAX_AGE_DAYS: 3
        run: |
          set -euo pipefail

          if [ -z "$LATEST" ]; then
            echo "::error::No log entries at all. Either logging has never delivered, or the query is reading the wrong location."
            exit 1
          fi

          AGE_DAYS=$(( ( $(date -u +%s) - $(date -u -d "$LATEST" +%s) ) / 86400 ))

          if [ "$AGE_DAYS" -gt "$MAX_AGE_DAYS" ]; then
            echo "::error::Newest log entry is $LATEST, $AGE_DAYS days old. Log delivery has likely stopped; check that the awslogsdelivery ACL grant on knaix-releases-logs is intact. Refusing to publish a figure that would look current."
            exit 1
          fi

          echo "Newest log entry $LATEST, $AGE_DAYS days old."

      # The badge shows the deduplicated count, which is the number that
      # survives being asked how it was measured. Both are in the file so the
      # sites can render either without another deploy.
      - name: Build stats.json
        env:
          TOTAL: ${{ steps.query.outputs.total }}
          DEDUPED: ${{ steps.query.outputs.deduped }}
        run: |
          python3 - <<'PY'
          import json, os, datetime

          total = int(os.environ["TOTAL"])
          deduped = int(os.environ["DEDUPED"])

          def short(n):
              if n < 1000:
                  return str(n)
              if n < 1_000_000:
                  k = n / 1000
                  return f"{k:.1f}k" if k < 10 else f"{k:.0f}k"
              m = n / 1_000_000
              return f"{m:.1f}M" if m < 10 else f"{m:.0f}M"

          payload = {
              "schemaVersion": 1,
              "label": "downloads",
              "message": short(deduped),
              "color": "7C5CFF",
              "total": total,
              "unique": deduped,
              "updated": datetime.datetime.now(datetime.timezone.utc)
                             .strftime("%Y-%m-%dT%H:%M:%SZ"),
          }
          with open("stats.json", "w") as f:
              json.dump(payload, f)
              f.write("\n")
          PY

          cat stats.json

      - name: Publish
        env:
          DISTRIBUTION_ID: ${{ vars.KNAIX_RELEASES_DISTRIBUTION_ID }}
        run: |
          set -euo pipefail

          aws s3api put-object \
            --bucket "$RELEASES_BUCKET" \
            --key stats.json \
            --body stats.json \
            --content-type application/json \
            --cache-control 'public, max-age=300' > /dev/null

          aws cloudfront create-invalidation \
            --distribution-id "$DISTRIBUTION_ID" \
            --paths /stats.json > /dev/null

      - name: Summary
        run: |
          {
            echo "### Knaix CLI downloads"
            echo
            echo "- Total requests: ${{ steps.query.outputs.total }}"
            echo "- Deduplicated: ${{ steps.query.outputs.deduped }}"
            echo
            echo "Published to https://releases.knaix.com/stats.json"
          } >> "$GITHUB_STEP_SUMMARY"
